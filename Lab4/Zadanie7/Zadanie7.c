#include <stdio.h>

int main()
{
    int n , x=2;
    printf("Podaj liczbę:");
    scanf("%d", &n);
    while (x < n)
    {
        if (n % x = 0)
        {
            print("Liczba nie jest liczbą pierwszą");
            x == n
        }
        else:
    }





}